var fs = require('fs');

var rf = function(path,callback){
    fs.access(path,(err)=>{
        if(err){
            throw err
        }
        fs.readFile(path,'utf-8',(err,data)=>{
            callback(null,data)
        })
    })
}

module.exports = rf;